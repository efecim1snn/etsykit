[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "etsykit"
version = "0.1.0"
description = "Open-source Etsy seller automation CLI: bulk listings, order/tracking sync, and SEO analysis via the official Etsy Open API v3."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
keywords = ["etsy", "ecommerce", "automation", "cli", "seo", "print-on-demand"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Office/Business",
]
dependencies = [
    "httpx>=0.27",
    "typer>=0.12",
    "rich>=13.7",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.5"]

[project.urls]
Homepage = "https://github.com/efecim1snn/etsykit"
Issues = "https://github.com/efecim1snn/etsykit/issues"

[project.scripts]
etsykit = "etsykit.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["etsykit"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Typer's API is built on both of these:
#   B008  — typer.Option()/Argument() must be evaluated in the default, that is the contract.
#   UP045 — Typer resolves annotations at runtime; `X | None` breaks that on Python 3.9
#           even with `from __future__ import annotations`, so Optional[X] stays.
"etsykit/cli.py" = ["B008", "UP045"]

[tool.pytest.ini_options]
testpaths = ["tests"]
